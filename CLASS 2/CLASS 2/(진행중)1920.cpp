#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int N; // 비교 대상
    vector<int>comparison_target;
    int M; // 비교 진행 수
    vector<int>compare_num;

}